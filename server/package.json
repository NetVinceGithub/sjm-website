{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "build": "cd ../frontend && npm install && npm run build",
    "postinstall": "if [ \"$NODE_ENV\" != \"production\" ]; then npx --unsafe-perm playwright install && npx --unsafe-perm puppeteer browsers install chrome --platform=linux; fi",
    "install-chrome": "npx puppeteer browsers install chrome"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "axios": "^1.9.0",
    "bcryptjs": "^3.0.2",
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.9.2",
    "multer": "^1.4.5-lts.1",
    "mysql2": "^3.14.1",
    "node-cron": "^4.0.5",
    "nodemailer": "^7.0.3",
    "nodemon": "^3.1.9",
    "path": "^0.12.7",
    "playwright": "^1.52.0",
    "puppeteer": "^24.10.0",
    "sequelize": "^6.37.7"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}